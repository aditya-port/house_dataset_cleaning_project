# house_dataset_cleaning_project
Built a PostgreSQL data cleaning pipeline for real estate data by handling NULL values, splitting address fields, standardizing categorical values, and removing duplicates using window functions to produce a clean analytical dataset.
